#include "speed_config.hpp"

state get_current_state() {
  return current_state;
}

void set_current_state(state new_state, int new_speed) {
  current_speed = new_state;
  current_speed = new_speed;
}